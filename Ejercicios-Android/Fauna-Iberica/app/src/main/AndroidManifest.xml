<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="dm2e.jesus.faunaiberica">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FaunaIberica">

        <!-- Declaración de MainActivity como actividad principal -->
        <activity android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Declaración de CarnivorosActivity -->
        <activity android:name=".CarnivorosActivity" />

        <!-- Declaración de RapacesActivity -->
        <activity android:name=".RapacesActivity" />

        <!-- Declaración de DatosAnimales si también la estás utilizando -->
        <activity android:name=".DatosAnimales" />

    </application>

</manifest>
